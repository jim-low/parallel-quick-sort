#pragma once

// quick notes
// int* means integer pointer, which could also mean integer array


class QuickSort
{
public:
	QuickSort(int* unsorted_array);
	int* sort();
	void display(); // this is where all the fancy fancy stuff gon be shown

private:
	// this is where all the inner workings will be
};
